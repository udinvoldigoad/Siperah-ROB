# Salin file ini menjadi scripts/deploy.env lalu isi nilai aslinya.
# deploy.env TIDAK boleh di-commit (sudah masuk .gitignore) â€” repo ini public.
SSH_HOST="user@ip-server"
SSH_PORT="65002"
REMOTE_APP="~/apps/saibar"
SITE_URL="https://subdomain-anda.example.com"

