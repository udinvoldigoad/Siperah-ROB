{
  "name": "supabase-edge-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Supabase to reduce egress",
  "main": "src/index.js",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
